What happens when you initialize a repository? Why do you need to do it?
it creates a  repositary. add .git folder and metadata.

How is the staging area different from the working directory and the repository?
What value do you think it offers?
it is intermediate between before and after commit state. It allows to see what changes going to happen after commit even berfore actual commit.

How can you use the staging area to make sure you have one commit per logical
change?
By adding only one logical change to staging area and commiting it before addiing further logical change to staging area.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
Of we want to do some experiment without affecting the main master branch. If we want to have the working version of the experimental chaneg wothout affecting the main branch.

How do the diagrams help you visualize the branch structure?
It makes it clear with diagram. 

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
It incoprates changes with two branch. 
Because this is good way to track and represent the change.

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?
